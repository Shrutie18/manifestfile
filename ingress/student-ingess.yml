apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: student-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: student.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: student-microservice-service
            port:
              number: 80
